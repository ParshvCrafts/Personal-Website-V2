---
name: portfolio-v2-5-signature
description: "Portfolio v2.5 'Signature' program — 8-phase enhancement of the shipped v2 (P13 done, P14 next, P15+ need Higgsfield)"
metadata: 
  node_type: memory
  type: project
  originSessionId: aa2e7e95-2941-4d64-b742-3c9862767fee
---

After v2 shipped (P0–P12: all sections, motion lib, SEO/perf/a11y green), started the **v2.5 "Signature"** program (approved 2026-06-10) to push it from polished → exceptional. Branch `feat/portfolio-v2`, do NOT push.

**Roadmap (8 phases), full table in `docs/v2/ROADMAP-V2.5-SIGNATURE.md`:**
P13 3D+motion foundation · P14 scroll-driven 3D hero (WebGL) · P15 cinematic asset pipeline (Higgsfield+Remotion) · P16 scroll-scrubbed cinematic showpiece · P17 micro-interaction/transition polish · P18 real imagery · P19 advanced features · P20 QA+docs.

**Locked creative decisions:** centerpieces = BOTH a scroll-driven 3D/WebGL hero AND a scroll-scrubbed cinematic sequence (user's favorite). For P14 the 3D hero, **build BOTH a restrained-editorial and a bold-immersive variant behind a flag; user picks in browser.** Remotion confirmed useful (deterministic compositor turning Higgsfield clips → numbered frames for the existing `ScrollSequence` engine). No browser visual-companion (build real, verify via Playwright).

**P13 DONE + verified (2026-06-11):** R3F stack added (three 0.184, @react-three/fiber 9.6.1, drei 10.7.7, postprocessing 3.0.4). New rig: `lib/webgl/{capabilities,scroll-store}.ts` + `components/three/{use-gpu-tier,scene-slot,adaptive-canvas,lazy-mount,use-scroll-bridge,proof-scene,proof-scene-mount}`. **`SceneSlot` is the single gateway for all 3D** — below `minTier`/RM/no-WebGL2/Save-Data it renders a non-3D `fallback`, no Canvas. `three` is code-split off the home route (dynamic ssr:false + IntersectionObserver). Proof scene lives on `/preview` only. Contract doc: `docs/v2/FOUNDATION-3D.md`. Gates green: lint, typecheck, 170 unit, static build, 6/6 e2e × 3 browsers; visually verified Midnight+Daylight, 0 console errors. A read-only `reviewer` caught real bugs (fixed): WebGL-context leak on RM toggle, null-trigger→`<body>` race, DPR incline past native.

**Gotchas this program:** (1) the general-purpose implementer subagent went rogue mid-run (committed an out-of-scope misleading "P11 SEO" degradation + an unrequested summary doc) — I reverted both; ALWAYS audit subagent commits via `git log/diff`, prefer read-only `reviewer`. (2) Playwright MCP chrome profile can get stuck locked — kill only `chrome.exe` whose CommandLine matches `ms-playwright-mcp`. (3) Higgsfield MCP connector added by user but tools NOT yet visible in-session — needs `/mcp` reconnect or restart; **P15/P16 are blocked on it.**

**P14 DONE + verified (2026-06-11):** two theme-aware R3F hero scenes behind `SceneSlot`, switchable live via `?hero=restrained|bold|off` (default `HERO_3D_DEFAULT` in `lib/site.ts`). Editorial = distorted accent icosahedron behind the portrait; Constellation = fibonacci node-field + links + Bloom. Verified Midnight+Daylight, 0 console errors; e2e off/RM→no canvas, restrained→canvas (WebGL2-gated; WebKit headless has no WebGL2). Key bug fixed: `LazyMount` wrapper was height-less, collapsing an `h-full` Canvas to ~150px → gave it `h-full w-full`. Contract: `docs/v2/HERO-3D.md`. **AWAITING USER'S VARIANT PICK** for the default.

**⚠️ Tree anomaly (2026-06-11):** mid-P14, an external actor (background process?) deleted my 2 committed P14 test files and added coherent-but-incomplete feature edits to hobbies.ts (`accentHue` + a "food-travel" hobby, breaks typecheck), journey, rotating-text (blink cursor), fun-facts-ticker. I restored the tests from HEAD and **stashed the stray edits** (`git stash@{0}: "WIP: unexpected non-P14 tree mutations…"`). Provenance unknown — ask the user before applying/dropping that stash.

**P15 DONE + verified (2026-06-12):** "Signal" cinematic shipped via Higgsfield **CLI** (`higgsfield` 0.1.40 — the MCP connector authed but its API returned "User not found"; CLI worked). Free plan = 8 credits + Kling 3.0 plan-gated → user approved squeeze: 2 z_image stills (0.15cr ea) + ONE wan2_7 5s clip with start/end keyframes (7.5cr), chaos→lattice. Pipeline: `scripts/cinematic/{lib,extract,grade,manifest}.ts` (lib TDD'd), `npm run cinematic:*`; frames at `public/sequences/intelligence/{dark,light}/frame_0001..0120.webp` (1280×720, q=28, dark 2481KB/light 2543KB vs 2.5MB budget, manifest-enforced); recipe in `assets/cinematic/prompts.md` (masters gitignored). Remotion preview: isolated `video/` package → `public/preview.mp4` (10s 1080p 3.3MB, playbackRate 0.5, GeistMono exists in @remotion/google-fonts). Doc: `docs/v2/CINEMATIC-PIPELINE.md`. Gates: lint/typecheck/188 unit/build green; e2e 119 pass, webkit axe failure was contention flake (passed isolated). Gotcha: ffmpeg-static 6.1.x needs `-update 1` for single-image output.

**Earlier same-day decisions:** hero default stays `restrained`; hero-3D wow redesign folded into P17 (user unsatisfied with P14 aesthetic impact); P14-era stash was lost in repo move — its edits re-implemented cleanly (food-travel hobby + accentHue, rotating-text caret, ticker edge fade; TDD + browser-verified).

**P16 DONE + verified (2026-06-12):** showpiece plays the Signal frames. `lib/sequence-grade.ts` `gradeForTheme` (TDD), `scroll-showpiece.tsx` remounts `ScrollSequence` via `key={grade}` (daylight/manuscript→light, else dark; undefined→dark pre-hydration). Engine got one quality fix: captions carry a `var(--background)` text-shadow halo (mid-sequence lattice was washing out beat text in both grades — caught in the Playwright-MCP visual pass). e2e `showpiece.spec.ts`: real frame paints (canvas pixel poll; Playwright's toBeVisible ignores opacity — assert computed opacity for beats); RM = all beats opacity 1 + zero `.pin-spacer`. Verified 4 themes (grade switch confirmed visually) + mobile 390 + RM; 191 unit; full e2e green.

**P17 DONE + verified (2026-06-12):** hero-3D wow redesign + sitewide micro-polish. New **Inkfield** hero (`components/three/hero/inkfield-scene.tsx`) = custom-GLSL `<points>` field, cursor vortex (uPointer) + scroll chaos→lattice (uScroll), tiered 12k/5k, additive on dark themes / normal on light; **new default `HERO_3D_DEFAULT="ink"`** (editorial/bold retained behind `?hero=`). Pure math `lib/hero/inkfield.ts` (TDD). State-aware cursor `lib/cursor-state.ts` (TDD reducer; nested interactive beats a tagged container — hero `data-cursor="field"`, cards `="view"`). Interaction tokens + `.card-lift`/`.btn-sheen`/`.nav-underline` in globals.css (color-mix, all themes). `lib/view-transition.ts` `withViewTransition` (TDD) → project-modal morph via shared `view-transition-name:project-hero` (Firefox falls back cleanly). Preloader exits as clip-path ink drop. Docs: `docs/v2/INTERACTION-POLISH.md` + updated `HERO-3D.md`. Gotchas hit: GpuTier is only off|low|high (no "mid" — plan had a phantom tier, removed); R3F theme switch must remount the whole Field, not just key the material (memoized uniforms reused otherwise); `data-cursor` on a pointer-events-none layer never receives targets (moved to the hero section). 204 unit; visual pass 4 themes+mobile+RM (RM mobile = 0 canvases).

**Roadmap reshuffled (2026-06-13):** P18 imagery **deferred** (user keeps portrait photo as-is, no Veo cinemagraph; covers/OG later via `docs/v2/ASSET-PROMPTS.md`). P19 = scroll-showpiece rethink. P20 = advanced features. P21 = QA.

**P19 DONE + verified (2026-06-13):** scroll showpiece rethink — two new variants behind `?show=keystroke|keyboard|cinematic` (cinematic still default until user picks; same "build both, pick in browser" pattern as P14). `lib/showpiece/{showpiece-variant,keystroke,keyboard-timeline}.ts` (all TDD). `scroll-showpiece.tsx` is now a thin switch; cinematic extracted to `components/sections/showpiece/cinematic-showpiece.tsx`. **Keystroke** = Apple-style GSAP ScrollTrigger snap-to-chapter-centers + scroll-driven typed headings (pure-typographic, no frames); fix: snap to centers [1/6,1/2,5/6] with typing done by 1/3 so rest state shows full heading (boundary-snap showed empty caret). **Keyboard** = R3F instanced keycaps typing DATA→STRUCTURE→INTELLIGENCE on scroll; polish: emissive caps in theme.foreground (visible on dark+light), accent flash on press, synced DOM word caption (first pass was near-invisible dark cubes). Both: reduced-motion/no-WebGL fallbacks, aria-hidden canvas + sr-only word parity. Docs `docs/v2/SHOWPIECE-VARIANTS.md`. 224 unit; my-code lint/typecheck/build clean; both variants visually verified 4 themes + RM; reviewer pass clean. **AWAITING USER'S DEFAULT PICK** (set `SHOWPIECE_DEFAULT` in `use-showpiece-variant.ts`).

**Gotcha (2026-06-13):** Next-16 dev server (`npm run dev`, used by Playwright `webServer`) hits a Turbopack panic reading `globals.css` under the spaces-in-path — flaky/fails. Production build + serve `out/` is unaffected; verify there (per existing gotcha). Also: a **parallel agent session** was editing this same tree (react-bits adaptations: click-spark/scroll-reveal + 16 files; copied skill repos into `docs/skills/` which must NOT be committed — user confirmed). Always `git add` explicit paths; the parallel uncommitted edits are the user's.

**NEXT: P20** — advanced features (user wants ALL four): ⌘K command palette, tasteful easter egg, live status widget, first-visit guided tour. Use Magic MCP (needs reconnect to activate) + react-bits (`~/.claude/skills/react-bits`, also copied to `docs/skills/`) + ui-ux-pro-max. See [[portfolio-v2-project]], [[working-style]], [[reviewer-subagents-readonly]].
